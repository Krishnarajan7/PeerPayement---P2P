import 'package:flutter/material.dart';

class SyncScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Sync Data")),
      body: Center(child: Text("This is the Sync Screen")),
    );
  }
}
